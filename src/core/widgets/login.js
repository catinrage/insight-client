import Widget from '../interface/widget';
import Window from '../interface/window';

const LoginWindow = new Window('Login', 'person-circle', '60rem', '40rem');

export default new Widget('Login', 'person-circle', LoginWindow, true);
